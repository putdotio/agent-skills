The user says:

"Our put.io frontend test harness uses the globally installed putio CLI to read
paged fixture state and approve or seed reversible test state. CLI versions may
vary, and the separate putio-cli skill might not be installed. Define the
harness contract so it discovers capabilities instead of guessing commands and
cannot perform accidental writes."

Explain the standalone discovery, output, auth, paging, and mutation workflow.
