The user says:

"Review a response-parser change in the put.io TypeScript SDK, including its
contract tests. Also add an empty state to the web app, use the putio CLI to list
transfers, and inspect a production file in the browser. Keep the review
read-only."

Respond with the correct scope and next steps.
